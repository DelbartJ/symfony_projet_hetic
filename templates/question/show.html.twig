{% extends 'base.html.twig' %}


{% block title %}
    {{ parent() }}
    Question
{% endblock %}

{% block body %}

    {{ parent() }}

    <h1>{{ question }}</h1>

    <div>
        <h2>Il y a {{ answers|length }} réponses<h2>
        <ul>
            {% for answer in answers %}
                <li>{{ answer }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}